# Mercant AI — Cloudflare Pages
# ─────────────────────────────────────────────────────────────────────────────
# This file is read by the Cloudflare Pages dashboard.
# Build settings are configured IN the Cloudflare dashboard (see README).
#
# Dashboard → Workers & Pages → mercant → Settings → Builds & Deployments:
#   Framework preset:       Next.js
#   Build command:          npm run build
#   Build output directory: .next
#   Node.js version:        18
#
# Environment Variables → Add all keys from .env.example
# ─────────────────────────────────────────────────────────────────────────────

name = "mercant"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
